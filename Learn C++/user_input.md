# Taking Input in C++

We can read user input with `cin`.

Example:

```cpp
int age;
cout << "Enter your age: ";
cin >> age;
cout << "Your age is " << age << endl;
```

We can also read a name:

```cpp
string name;
cout << "Enter your name: ";
cin >> name;
cout << "Hello " << name << endl;
```
