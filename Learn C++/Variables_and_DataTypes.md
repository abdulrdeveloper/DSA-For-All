# Variables and Data Types

C++ variables store different kinds of data.

- `int` stores whole numbers.
- `float` and `double` store decimal numbers.
- `char` stores one character.
- `string` stores text.

Example:

```cpp
int age = 20;
float price = 4.99;
char grade = 'A';
string name = "Ali";
```
